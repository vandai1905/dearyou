# Chúc mừng Ngày 8/3 🎉

Trang web đơn giản để chúc mừng ngày Quốc tế Phụ nữ (8/3) dành cho người yêu của bạn. Bao gồm:
- Thiệp chúc
- Ảnh có hiệu ứng động lung linh (gallery)
- Nhạc nền dễ thương
- Hiệu ứng trái tim bay và ngôi sao lung linh
- Hộp nhập lời chúc cá nhân

## Cấu trúc dự án

```
index.html
styles.css
script.js
assets/
  images/
    placeholder.jpg (thay bằng ảnh thật; có thể thêm nhiều ảnh để gallery tự động hiển thị)
  audio/
    music.mp3 (thêm bài hát của bạn)
```

## Các tính năng nổi bật

- **Gallery:** hiển thị nhiều ảnh cùng lúc, bạn có thể bổ sung thêm file ảnh vào thư mục `assets/images`.
- **Lời chúc cá nhân:** nhập tin nhắn của bạn và bấm nút để hiển thị trong trang.
- **Hệ thống trái tim:** các trái tim nhỏ sẽ bay nhẹ nhàng lên nền, tạo cảm giác lãng mạn.
- **Nhạc và hiệu ứng:** nút bật/tắt điều khiển nhạc, ảnh trong gallery sẽ glow khi nhạc phát.

## Hướng dẫn dùng

1. **Chuẩn bị tài nguyên**
   - Đổi ảnh trong `assets/images` (có thể thêm nhiều ảnh cho gallery).
   - Chép file nhạc MP3 vào `assets/audio/music.mp3`.
2. **Mở trang**
   - Mở `index.html` bằng cách kéo thả vào trình duyệt, hay dùng `xdg-open` trên Linux.
   - Click nút "Bật nhạc" để phát/ dừng nhạc và kích hoạt hiệu ứng lung linh.
   - Nhập một lời chúc vào ô rồi bấm "Hiện lời chúc" để nó xuất hiện ngay dưới.
3. **Tùy chỉnh**
   - Sửa nội dung trong `index.html` để thay đổi lời chúc, tiêu đề, thêm phần mới.
   - Chỉnh CSS trong `styles.css` để thay đổi màu sắc, kích thước hay thêm animation.
   - Thêm logic JavaScript trong `script.js` nếu muốn tạo thêm hiệu ứng (confetti, hộp quà, v.v.).

> **Lưu ý:** trình duyệt thường chặn nhạc tự động; bạn cần tương tác (click) để âm thanh chạy.

Chúc bạn có một món quà web thật ngọt ngào và ấm áp dành tặng người yêu! 💕